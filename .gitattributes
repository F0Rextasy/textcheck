# Byte-exact fixtures: textcheck tests depend on exact CRLF / lone-CR /
# BOM bytes surviving the git round-trip on every platform, so Git must
# never normalize anything under fixtures/ (no checkout conversion either).
fixtures/** -text
